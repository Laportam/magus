<!DOCTYPE html>
<html lang="en">
<%- include('partials/head')  %> 
<body>
    <h1>Ya creaste tu producto <u><%= product.name %></u></h1>
    <h2>Ahora solo falta subir sus respectivas imágenes:</h2>
    <form action="/catalogo/create/<%= product.id %>" method="POST" enctype="multipart/form-data">
        <label for="image">Imágenes</label>
        <br>
        <input name="image" type="file" value="<%= locals.oldData ? oldData.name : null %>" multiple>
        <% if (locals.errors && locals.success) { %>
            <div class="text-danger">
                <%= success.msg %>
                <br>
                <%= errors.msg %> 
            </div>
        <% } else if (locals.errors) { %> 
            <div class="text-danger">
                <%= errors.msg %> 
            </div>
        <% } %> 
        <br>
        <br>
        <hr>
        <button type="submit">¡Crear producto!</button>
    </form>
</body>
</html>